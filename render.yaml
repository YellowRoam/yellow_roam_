services:
  - type: web
    name: yellowroam
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    region: oregon
    autoDeploy: true

  
